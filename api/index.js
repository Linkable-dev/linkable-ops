// Vercel's entry point. Everything it serves is defined in server/app.js —
// see the note there about why this file is four lines rather than a second
// copy of the route table.
import app from "../server/app.js";

export default app;
