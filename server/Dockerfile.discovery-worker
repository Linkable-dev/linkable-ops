FROM node:20-slim

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci --omit=dev --no-audit --no-fund

COPY automation ./automation
COPY lib ./lib
COPY data ./data

ENV NODE_ENV=production

CMD ["node", "automation/run-discovery-worker.js", "--drain"]
